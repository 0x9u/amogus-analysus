# 🧯 AmogUs Analysus 🔪🩸

if we need python packages:
- do `python3 -m venv .venv` once
- do `source .venv/bin/activate` to activate the venv every time you need to use python or pip
- do `pip install -r requirements.txt` to update deps because e.g. someone else added something
- do `pip install numpy` to add numpy or whatever
- do `pip freeze > requirements.txt` if you add anything and commit requirements.txt after
